# Hardware and device settings
_: {
  programs.dank-material-shell.settings = {
    # Launch prefix for apps
    launchPrefix = "";

    # Device pins
    brightnessDevicePins = { };
    audioInputDevicePins = { };
    audioOutputDevicePins = { };
  };
}
